# NovaChat V1 — 阶段五实现计划：聊天模块

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成实时文字聊天：服务端路由消息 + 推送给在线用户；客户端支持发送/接收消息、历史记录（分页）、最近会话列表；UI 层实现气泡式 ChatView 和 ConversationList。

**Architecture:** ChatService 在主线程处理所有聊天业务；发送消息通过 SeqId 回调确认，服务端推送（MsgPush，seq_id=0）经 `TcpClient::packetReceived` 信号分发。会话 ID 用确定性规则 `min(a,b):max(a,b)` 生成，无需服务端分配。ChatView 使用 `QListView` + 自定义 `MessageBubbleDelegate`（气泡），ConversationList 使用 `QListWidget`。MainWindow 左侧改为 QTabWidget（会话/联系人），右侧为 ChatView。

**Tech Stack:** C++20 · Qt6 Widgets · Protobuf（chat.proto）· SQLite · spdlog

---

## 本阶段新增/修改文件

```
NovaChat/
├── proto/
│   └── chat.proto                          ← 完善消息定义
├── server/
│   └── src/
│       ├── main.cpp                        ← 修改：路由 Chat 包
│       ├── storage/
│       │   ├── ServerStorage.h             ← 修改：会话注册 + 消息存储
│       │   └── ServerStorage.cpp           ← 修改
│       └── handler/
│           ├── AuthHandler.cpp             ← 修改：登录后注册 session
│           ├── ChatHandler.h               ← 新增
│           └── ChatHandler.cpp             ← 新增
└── client/
    ├── CMakeLists.txt                      ← 修改
    ├── src/
    │   ├── main.cpp                        ← 修改：创建 ChatService
    │   ├── storage/
    │   │   ├── MessageRepository.h         ← 新增
    │   │   ├── MessageRepository.cpp       ← 新增
    │   │   ├── ConversationRepository.h    ← 新增
    │   │   └── ConversationRepository.cpp  ← 新增
    │   ├── service/
    │   │   ├── ChatService.h               ← 新增
    │   │   └── ChatService.cpp             ← 新增
    │   └── ui/
    │       ├── MainWindow.h                ← 修改：集成两侧面板
    │       ├── MainWindow.cpp              ← 修改
    │       ├── ConversationList.h          ← 新增
    │       ├── ConversationList.cpp        ← 新增
    │       ├── ChatView.h                  ← 新增
    │       ├── ChatView.cpp                ← 新增
    │       ├── MessageBubbleDelegate.h     ← 新增
    │       └── MessageBubbleDelegate.cpp   ← 新增
    └── tests/
        ├── CMakeLists.txt                  ← 修改
        └── storage/
            ├── test_message_repository.cpp   ← 新增
            └── test_conversation_repository.cpp ← 新增
```

---

## Task 23：服务端聊天支持

**Files:**
- Modify: `proto/chat.proto`
- Modify: `server/src/storage/ServerStorage.h`
- Modify: `server/src/storage/ServerStorage.cpp`
- Modify: `server/src/handler/AuthHandler.cpp`
- Create: `server/src/handler/ChatHandler.h`
- Create: `server/src/handler/ChatHandler.cpp`
- Modify: `server/src/main.cpp`
- Modify: `server/CMakeLists.txt`

- [ ] **Step 1：完善 proto/chat.proto**

```protobuf
syntax = "proto3";
package novachat;

// 会话 ID 由客户端生成：min(sender_id, receiver_id) + ":" + max(...)

message SendMessageRequest {
    string token        = 1;
    string local_msg_id = 2;   // 客户端临时 ID，服务端原样回传
    string receiver_id  = 3;
    string content      = 4;
}

message SendMessageResponse {
    string local_msg_id    = 1;   // 与请求中一致
    string server_msg_id   = 2;   // 服务端分配的持久化 ID
    string conversation_id = 3;
    int64  timestamp_ms    = 4;
}

// 服务端推送给消息接收方（seq_id = 0）
message MessagePush {
    string message_id      = 1;
    string conversation_id = 2;
    string sender_id       = 3;
    string content         = 4;
    int64  timestamp_ms    = 5;
}

message HistoryMessageRecord {
    string message_id      = 1;
    string conversation_id = 2;
    string sender_id       = 3;
    string content         = 4;
    int64  timestamp_ms    = 5;
}

message GetHistoryRequest {
    string token            = 1;
    string conversation_id  = 2;
    int64  before_timestamp = 3;   // 0 = 最新；非零 = 加载该时间戳之前的消息
    int32  limit            = 4;   // 最大 50
}

message GetHistoryResponse {
    repeated HistoryMessageRecord messages = 1;
    bool has_more                          = 2;
}

message GetConversationsRequest {
    string token = 1;
}

message ServerConversationRecord {
    string conversation_id      = 1;
    string peer_user_id         = 2;
    string last_message_preview = 3;
    int64  last_message_time    = 4;
}

message GetConversationsResponse {
    repeated ServerConversationRecord conversations = 1;
}
```

- [ ] **Step 2：在 server/src/storage/ServerStorage.h 添加 chat 数据结构和方法**

在 `public:` 区块追加：
```cpp
    // ── 在线会话注册（用于消息推送）──────────────────────────────
    void registerSession  (const std::string& user_id,
                           std::shared_ptr<ClientSession> session);
    void unregisterSession(const std::string& user_id);
    std::shared_ptr<ClientSession> findSession(const std::string& user_id);

    // ── 消息存储 ─────────────────────────────────────────────────
    struct MsgRecord {
        std::string message_id;
        std::string conversation_id;
        std::string sender_id;
        std::string content;
        int64_t     timestamp_ms{};
    };

    // 保存消息并返回服务端分配的 message_id（16 位 hex）
    std::string saveMessage(MsgRecord msg);

    // 返回指定会话 before_timestamp 之前（含）的消息，最多 limit 条，倒序
    std::vector<MsgRecord> getHistory(const std::string& conv_id,
                                       int64_t before_ts, int limit);

    // 返回用户参与过的会话列表（去重，按最新消息时间排序）
    std::vector<MsgRecord> getLatestMessages(const std::string& user_id);
```

在 `private:` 区块追加：
```cpp
    // key: user_id → active ClientSession
    std::unordered_map<std::string,
                       std::weak_ptr<ClientSession>> active_sessions_;

    // key: conversation_id → 消息列表（按 timestamp_ms 升序）
    std::unordered_map<std::string, std::vector<MsgRecord>> messages_;
```

在文件顶部添加：
```cpp
#include "network/ClientSession.h"
#include <cstdint>
```

- [ ] **Step 3：在 server/src/storage/ServerStorage.cpp 末尾追加 chat 实现**

```cpp
void ServerStorage::registerSession(const std::string& user_id,
                                     std::shared_ptr<ClientSession> session) {
    std::lock_guard<std::mutex> lock(mutex_);
    active_sessions_[user_id] = session;
}

void ServerStorage::unregisterSession(const std::string& user_id) {
    std::lock_guard<std::mutex> lock(mutex_);
    active_sessions_.erase(user_id);
}

std::shared_ptr<ClientSession>
ServerStorage::findSession(const std::string& user_id) {
    std::lock_guard<std::mutex> lock(mutex_);
    auto it = active_sessions_.find(user_id);
    if (it == active_sessions_.end()) return nullptr;
    return it->second.lock();   // weak_ptr 转 shared_ptr
}

std::string ServerStorage::saveMessage(MsgRecord msg) {
    std::lock_guard<std::mutex> lock(mutex_);
    msg.message_id = generateHex(8);   // 16 字符 hex
    messages_[msg.conversation_id].push_back(msg);
    return msg.message_id;
}

std::vector<ServerStorage::MsgRecord>
ServerStorage::getHistory(const std::string& conv_id,
                           int64_t before_ts, int limit) {
    std::lock_guard<std::mutex> lock(mutex_);
    auto it = messages_.find(conv_id);
    if (it == messages_.end()) return {};

    const auto& msgs = it->second;
    std::vector<MsgRecord> result;
    // 从最新到最旧遍历
    for (int i = static_cast<int>(msgs.size()) - 1; i >= 0; --i) {
        if (before_ts > 0 && msgs[i].timestamp_ms >= before_ts) continue;
        result.push_back(msgs[i]);
        if (static_cast<int>(result.size()) >= limit) break;
    }
    // 结果按升序返回
    std::reverse(result.begin(), result.end());
    return result;
}

std::vector<ServerStorage::MsgRecord>
ServerStorage::getLatestMessages(const std::string& user_id) {
    std::lock_guard<std::mutex> lock(mutex_);
    // 找出 user_id 参与的所有会话（conv_id 包含 user_id）
    std::vector<MsgRecord> latest;
    for (const auto& [conv_id, msgs] : messages_) {
        if (conv_id.find(user_id) == std::string::npos) continue;
        if (!msgs.empty()) latest.push_back(msgs.back());
    }
    // 按时间倒序
    std::sort(latest.begin(), latest.end(),
              [](const MsgRecord& a, const MsgRecord& b) {
                  return a.timestamp_ms > b.timestamp_ms;
              });
    return latest;
}
```

- [ ] **Step 4：修改 server/src/handler/AuthHandler.cpp — 登录成功后注册 session**

在 `AuthHandler::onLogin` 成功分支（发送 LoginResponse 之前）追加：
```cpp
    storage_.registerSession(user->user_id, session);
    spdlog::info("Session registered: {}", user->username);
```

同时，ClientSession 析构或断开时需注销。在 `server/src/network/ClientSession.cpp` 中，在连接断开的清理代码处（或析构函数中）触发注销。由于 ClientSession 不持有 ServerStorage 引用，最简单的方式是在 AuthHandler 中保存 user_id→session 弱引用后，在 TcpServer 的断开回调中调用 `storage_.unregisterSession(user_id)`。

V1 简化方案：ServerStorage 使用 `weak_ptr`（Step 2 已设计），`findSession` 时若 `weak_ptr` 已过期则自动失效，推送时检查空指针即可。不需要显式注销。

- [ ] **Step 5：写 server/src/handler/ChatHandler.h**

```cpp
#pragma once
#include "network/ClientSession.h"
#include "storage/ServerStorage.h"
#include <chrono>
#include <memory>

class ChatHandler {
public:
    explicit ChatHandler(ServerStorage& storage);

    void handle(std::shared_ptr<ClientSession> session, const Packet& packet);

private:
    void onSendMessage      (std::shared_ptr<ClientSession> session, const Packet& pkt);
    void onGetHistory       (std::shared_ptr<ClientSession> session, const Packet& pkt);
    void onGetConversations (std::shared_ptr<ClientSession> session, const Packet& pkt);

    std::optional<ServerStorage::UserRecord>
    validateToken(std::shared_ptr<ClientSession> session,
                  const std::string& token, uint32_t seq_id);

    void sendError(std::shared_ptr<ClientSession> session,
                   uint32_t seq_id, int code, const std::string& msg);

    static std::string makeConvId(const std::string& a, const std::string& b);

    static int64_t nowMs();

    ServerStorage& storage_;
};
```

- [ ] **Step 6：写 server/src/handler/ChatHandler.cpp**

```cpp
#include "handler/ChatHandler.h"
#include "proto/MessageType.h"
#include "chat.pb.h"
#include "common.pb.h"
#include <algorithm>
#include <spdlog/spdlog.h>

ChatHandler::ChatHandler(ServerStorage& storage) : storage_(storage) {}

int64_t ChatHandler::nowMs() {
    using namespace std::chrono;
    return duration_cast<milliseconds>(
        system_clock::now().time_since_epoch()).count();
}

std::string ChatHandler::makeConvId(const std::string& a,
                                     const std::string& b) {
    return (a < b) ? (a + ":" + b) : (b + ":" + a);
}

void ChatHandler::handle(std::shared_ptr<ClientSession> session,
                          const Packet& packet) {
    switch (packet.type) {
    case MessageType::SendMessageRequest:   onSendMessage    (session, packet); break;
    case MessageType::GetHistoryRequest:    onGetHistory     (session, packet); break;
    case MessageType::GetConversationsRequest: onGetConversations(session, packet); break;
    default: break;
    }
}

std::optional<ServerStorage::UserRecord>
ChatHandler::validateToken(std::shared_ptr<ClientSession> session,
                            const std::string& token, uint32_t seq_id) {
    auto user = storage_.findByToken(token);
    if (!user) sendError(session, seq_id, novachat::AUTH_EXPIRED, "Token 无效");
    return user;
}

void ChatHandler::sendError(std::shared_ptr<ClientSession> session,
                             uint32_t seq_id, int code, const std::string& msg) {
    novachat::ErrorResponse er;
    er.set_code(static_cast<novachat::ErrorCode>(code));
    er.set_message(msg);
    Packet resp;
    resp.type   = MessageType::ErrorResponse;
    resp.seq_id = seq_id;
    resp.payload.resize(er.ByteSizeLong());
    er.SerializeToArray(resp.payload.data(), static_cast<int>(resp.payload.size()));
    session->sendPacket(std::move(resp));
}

void ChatHandler::onSendMessage(std::shared_ptr<ClientSession> session,
                                 const Packet& pkt) {
    novachat::SendMessageRequest req;
    if (!req.ParseFromArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()))) {
        sendError(session, pkt.seq_id, novachat::UNKNOWN_ERROR, "parse error");
        return;
    }
    auto sender = validateToken(session, req.token(), pkt.seq_id);
    if (!sender) return;

    const std::string conv_id = makeConvId(sender->user_id, req.receiver_id());
    const int64_t     ts      = nowMs();

    ServerStorage::MsgRecord record;
    record.conversation_id = conv_id;
    record.sender_id       = sender->user_id;
    record.content         = req.content();
    record.timestamp_ms    = ts;
    const std::string server_id = storage_.saveMessage(record);

    // ── 回复发送方 ────────────────────────────────────────────────
    novachat::SendMessageResponse resp_pb;
    resp_pb.set_local_msg_id   (req.local_msg_id());
    resp_pb.set_server_msg_id  (server_id);
    resp_pb.set_conversation_id(conv_id);
    resp_pb.set_timestamp_ms   (ts);

    Packet resp;
    resp.type   = MessageType::SendMessageResponse;
    resp.seq_id = pkt.seq_id;
    resp.payload.resize(resp_pb.ByteSizeLong());
    resp_pb.SerializeToArray(resp.payload.data(), static_cast<int>(resp.payload.size()));
    session->sendPacket(std::move(resp));

    // ── 推送给接收方（若在线）────────────────────────────────────
    auto recv_session = storage_.findSession(req.receiver_id());
    if (recv_session) {
        novachat::MessagePush push;
        push.set_message_id     (server_id);
        push.set_conversation_id(conv_id);
        push.set_sender_id      (sender->user_id);
        push.set_content        (req.content());
        push.set_timestamp_ms   (ts);

        Packet push_pkt;
        push_pkt.type    = MessageType::MessagePush;
        push_pkt.seq_id  = 0;   // 服务端主动推送
        push_pkt.payload.resize(push.ByteSizeLong());
        push.SerializeToArray(push_pkt.payload.data(),
                              static_cast<int>(push_pkt.payload.size()));
        recv_session->sendPacket(std::move(push_pkt));
    }

    spdlog::info("Message {} → {} (conv={})", sender->username,
                 req.receiver_id(), conv_id);
}

void ChatHandler::onGetHistory(std::shared_ptr<ClientSession> session,
                                const Packet& pkt) {
    novachat::GetHistoryRequest req;
    if (!req.ParseFromArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()))) {
        sendError(session, pkt.seq_id, novachat::UNKNOWN_ERROR, "parse error");
        return;
    }
    auto user = validateToken(session, req.token(), pkt.seq_id);
    if (!user) return;

    const int limit = std::clamp(req.limit(), 1, 50);
    auto msgs = storage_.getHistory(req.conversation_id(),
                                    req.before_timestamp(), limit);

    novachat::GetHistoryResponse resp_pb;
    for (const auto& m : msgs) {
        auto* r = resp_pb.add_messages();
        r->set_message_id     (m.message_id);
        r->set_conversation_id(m.conversation_id);
        r->set_sender_id      (m.sender_id);
        r->set_content        (m.content);
        r->set_timestamp_ms   (m.timestamp_ms);
    }
    resp_pb.set_has_more(static_cast<int>(msgs.size()) == limit);

    Packet resp;
    resp.type   = MessageType::GetHistoryResponse;
    resp.seq_id = pkt.seq_id;
    resp.payload.resize(resp_pb.ByteSizeLong());
    resp_pb.SerializeToArray(resp.payload.data(), static_cast<int>(resp.payload.size()));
    session->sendPacket(std::move(resp));
}

void ChatHandler::onGetConversations(std::shared_ptr<ClientSession> session,
                                      const Packet& pkt) {
    novachat::GetConversationsRequest req;
    if (!req.ParseFromArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()))) {
        sendError(session, pkt.seq_id, novachat::UNKNOWN_ERROR, "parse error");
        return;
    }
    auto user = validateToken(session, req.token(), pkt.seq_id);
    if (!user) return;

    auto latest_msgs = storage_.getLatestMessages(user->user_id);

    novachat::GetConversationsResponse resp_pb;
    for (const auto& m : latest_msgs) {
        auto* r = resp_pb.add_conversations();
        r->set_conversation_id (m.conversation_id);
        // peer = 另一方的 user_id（conv_id 格式：min:max）
        const auto colon = m.conversation_id.find(':');
        const std::string id_a = m.conversation_id.substr(0, colon);
        const std::string id_b = m.conversation_id.substr(colon + 1);
        r->set_peer_user_id(id_a == user->user_id ? id_b : id_a);
        r->set_last_message_preview(m.content.size() > 50
            ? m.content.substr(0, 50) + "..." : m.content);
        r->set_last_message_time(m.timestamp_ms);
    }

    Packet resp;
    resp.type   = MessageType::GetConversationsResponse;
    resp.seq_id = pkt.seq_id;
    resp.payload.resize(resp_pb.ByteSizeLong());
    resp_pb.SerializeToArray(resp.payload.data(), static_cast<int>(resp.payload.size()));
    session->sendPacket(std::move(resp));
}
```

- [ ] **Step 7：更新 server/CMakeLists.txt 和 server/src/main.cpp**

`server/CMakeLists.txt` 追加 `src/handler/ChatHandler.cpp`。

`server/src/main.cpp` 添加：
```cpp
#include "handler/ChatHandler.h"
// ...
ChatHandler chat_handler(storage);
// 在 switch 中追加：
case MessageType::SendMessageRequest:
case MessageType::GetHistoryRequest:
case MessageType::GetConversationsRequest:
    chat_handler.handle(session, pkt);
    break;
```

同时在 `proto/MessageType.h` 确认已定义以下枚举值（阶段二应已定义，若未定义则追加）：
```cpp
SendMessageRequest    = 0x0201,
SendMessageResponse   = 0x0202,
MessagePush           = 0x0203,
GetHistoryRequest     = 0x0204,
GetHistoryResponse    = 0x0205,
GetConversationsRequest  = 0x0206,
GetConversationsResponse = 0x0207,
```

- [ ] **Step 8：编译验证**

```
cmake --build --preset windows-debug
```

预期：编译无报错。

- [ ] **Step 9：提交**

```
git add proto/chat.proto server/src/storage/ server/src/handler/ChatHandler.h
git add server/src/handler/ChatHandler.cpp server/src/handler/AuthHandler.cpp
git add server/src/main.cpp server/CMakeLists.txt
git commit -m "feat(server): 添加 ChatHandler，支持消息发送/推送/历史/会话列表"
```

---

## Task 24：客户端 MessageRepository + 单元测试

**Files:**
- Create: `client/src/storage/MessageRepository.h`
- Create: `client/src/storage/MessageRepository.cpp`
- Create: `client/tests/storage/test_message_repository.cpp`

- [ ] **Step 1：写 client/src/storage/MessageRepository.h**

```cpp
#pragma once
#include "core/Message.h"
#include "storage/DatabaseManager.h"
#include <optional>
#include <string>
#include <vector>

// 操作 messages 表
// 所有方法必须在 DB 线程调用
class MessageRepository {
public:
    explicit MessageRepository(DatabaseManager& db);

    // 插入一条消息（message_id 唯一，ON CONFLICT IGNORE）
    void save(const Message& msg);

    // 将临时 local_id 替换为服务端分配的 server_id，并更新 status=1
    void confirmSent(const std::string& local_id,
                     const std::string& server_id,
                     int64_t            server_timestamp_ms);

    // 更新消息状态（0=发送中 1=已发送 2=失败）
    void updateStatus(const std::string& message_id, int status);

    // 查询会话的历史消息：before_timestamp==0 取最新；分页每次最多 limit 条，升序
    std::vector<Message> findByConversation(const std::string& conversation_id,
                                             int64_t before_timestamp_ms = 0,
                                             int     limit               = 50);

    std::optional<Message> findById(const std::string& message_id);

private:
    DatabaseManager& db_;
    static Message rowToMessage(sqlite3_stmt* stmt);
};
```

- [ ] **Step 2：写 client/src/storage/MessageRepository.cpp**

```cpp
#include "storage/MessageRepository.h"
#include <sqlite3.h>

MessageRepository::MessageRepository(DatabaseManager& db) : db_(db) {}

Message MessageRepository::rowToMessage(sqlite3_stmt* stmt) {
    Message m;
    m.message_id      = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 0));
    m.conversation_id = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1));
    m.sender_id       = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2));
    m.content         = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 3));
    m.type            = static_cast<MessageType>(sqlite3_column_int(stmt, 4));
    m.timestamp_ms    = sqlite3_column_int64(stmt, 5);
    m.status          = static_cast<MessageStatus>(sqlite3_column_int(stmt, 6));
    return m;
}

void MessageRepository::save(const Message& m) {
    const char* sql = R"(
        INSERT OR IGNORE INTO messages
            (message_id, conversation_id, sender_id, content,
             msg_type, timestamp, status)
        VALUES (?,?,?,?,?,?,?)
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text (stmt, 1, m.message_id.c_str(),      -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 2, m.conversation_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 3, m.sender_id.c_str(),       -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 4, m.content.c_str(),         -1, SQLITE_STATIC);
    sqlite3_bind_int  (stmt, 5, static_cast<int>(m.type));
    sqlite3_bind_int64(stmt, 6, m.timestamp_ms);
    sqlite3_bind_int  (stmt, 7, static_cast<int>(m.status));
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

void MessageRepository::confirmSent(const std::string& local_id,
                                     const std::string& server_id,
                                     int64_t            server_ts) {
    const char* sql = R"(
        UPDATE messages
        SET message_id = ?, timestamp = ?, status = 1
        WHERE message_id = ?
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text (stmt, 1, server_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_bind_int64(stmt, 2, server_ts);
    sqlite3_bind_text (stmt, 3, local_id.c_str(),  -1, SQLITE_STATIC);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

void MessageRepository::updateStatus(const std::string& message_id, int status) {
    const char* sql = "UPDATE messages SET status = ? WHERE message_id = ?";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_int (stmt, 1, status);
    sqlite3_bind_text(stmt, 2, message_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

std::vector<Message>
MessageRepository::findByConversation(const std::string& conv_id,
                                       int64_t before_ts,
                                       int limit) {
    const char* sql_latest = R"(
        SELECT message_id, conversation_id, sender_id, content,
               msg_type, timestamp, status
        FROM messages WHERE conversation_id = ?
        ORDER BY timestamp DESC LIMIT ?
    )";
    const char* sql_before = R"(
        SELECT message_id, conversation_id, sender_id, content,
               msg_type, timestamp, status
        FROM messages WHERE conversation_id = ? AND timestamp < ?
        ORDER BY timestamp DESC LIMIT ?
    )";

    sqlite3_stmt* stmt;
    if (before_ts == 0) {
        sqlite3_prepare_v2(db_.handle(), sql_latest, -1, &stmt, nullptr);
        sqlite3_bind_text(stmt, 1, conv_id.c_str(), -1, SQLITE_STATIC);
        sqlite3_bind_int (stmt, 2, limit);
    } else {
        sqlite3_prepare_v2(db_.handle(), sql_before, -1, &stmt, nullptr);
        sqlite3_bind_text (stmt, 1, conv_id.c_str(), -1, SQLITE_STATIC);
        sqlite3_bind_int64(stmt, 2, before_ts);
        sqlite3_bind_int  (stmt, 3, limit);
    }

    std::vector<Message> result;
    while (sqlite3_step(stmt) == SQLITE_ROW) {
        result.push_back(rowToMessage(stmt));
    }
    sqlite3_finalize(stmt);
    // 查询结果是 DESC，反转为 ASC 展示
    std::reverse(result.begin(), result.end());
    return result;
}

std::optional<Message>
MessageRepository::findById(const std::string& message_id) {
    const char* sql = R"(
        SELECT message_id, conversation_id, sender_id, content,
               msg_type, timestamp, status
        FROM messages WHERE message_id = ? LIMIT 1
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text(stmt, 1, message_id.c_str(), -1, SQLITE_STATIC);
    std::optional<Message> result;
    if (sqlite3_step(stmt) == SQLITE_ROW) result = rowToMessage(stmt);
    sqlite3_finalize(stmt);
    return result;
}
```

- [ ] **Step 3：写 client/tests/storage/test_message_repository.cpp**

```cpp
#include <catch2/catch_test_macros.hpp>
#include "storage/DatabaseManager.h"
#include "storage/MessageRepository.h"

static Message makeMsg(const std::string& id,
                        const std::string& conv,
                        const std::string& sender,
                        int64_t ts,
                        MessageStatus status = MessageStatus::Sending) {
    Message m;
    m.message_id      = id;
    m.conversation_id = conv;
    m.sender_id       = sender;
    m.content         = "Hello from " + sender;
    m.type            = MessageType::Text;
    m.timestamp_ms    = ts;
    m.status          = status;
    return m;
}

TEST_CASE("MessageRepository save 和 findById", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("m1", "conv_a", "alice", 1000));
    auto found = repo.findById("m1");

    REQUIRE(found.has_value());
    CHECK(found->content    == "Hello from alice");
    CHECK(found->timestamp_ms == 1000);
}

TEST_CASE("MessageRepository 重复 message_id 忽略（不抛异常）", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("m1", "conv_a", "alice", 1000));
    REQUIRE_NOTHROW(repo.save(makeMsg("m1", "conv_a", "alice", 2000)));

    // 原始时间戳保持不变
    auto found = repo.findById("m1");
    REQUIRE(found.has_value());
    CHECK(found->timestamp_ms == 1000);
}

TEST_CASE("MessageRepository findByConversation 按时间升序", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("m3", "conv_a", "alice", 3000));
    repo.save(makeMsg("m1", "conv_a", "bob",   1000));
    repo.save(makeMsg("m2", "conv_a", "alice", 2000));

    auto msgs = repo.findByConversation("conv_a");
    REQUIRE(msgs.size() == 3);
    CHECK(msgs[0].message_id == "m1");
    CHECK(msgs[1].message_id == "m2");
    CHECK(msgs[2].message_id == "m3");
}

TEST_CASE("MessageRepository findByConversation 分页 before_timestamp", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    for (int i = 1; i <= 5; ++i)
        repo.save(makeMsg("m" + std::to_string(i), "conv_a", "alice",
                          static_cast<int64_t>(i) * 1000));

    // 获取 timestamp < 4000 的消息，最多 2 条
    auto msgs = repo.findByConversation("conv_a", 4000, 2);
    REQUIRE(msgs.size() == 2);
    CHECK(msgs[0].message_id == "m2");
    CHECK(msgs[1].message_id == "m3");
}

TEST_CASE("MessageRepository updateStatus", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("m1", "conv_a", "alice", 1000, MessageStatus::Sending));
    repo.updateStatus("m1", static_cast<int>(MessageStatus::Sent));

    auto found = repo.findById("m1");
    REQUIRE(found.has_value());
    CHECK(found->status == MessageStatus::Sent);
}

TEST_CASE("MessageRepository confirmSent 替换 local_id 并更新时间戳", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("local_tmp_001", "conv_a", "alice", 1000, MessageStatus::Sending));
    repo.confirmSent("local_tmp_001", "server_abc", 9999);

    CHECK(!repo.findById("local_tmp_001").has_value());  // local_id 已不存在
    auto found = repo.findById("server_abc");
    REQUIRE(found.has_value());
    CHECK(found->timestamp_ms == 9999);
    CHECK(found->status       == MessageStatus::Sent);
}

TEST_CASE("MessageRepository findByConversation 不同会话隔离", "[msg_repo]") {
    DatabaseManager   db(":memory:");
    MessageRepository repo(db);

    repo.save(makeMsg("m1", "conv_a", "alice", 1000));
    repo.save(makeMsg("m2", "conv_b", "bob",   1000));

    CHECK(repo.findByConversation("conv_a").size() == 1);
    CHECK(repo.findByConversation("conv_b").size() == 1);
    CHECK(repo.findByConversation("conv_c").empty());
}
```

- [ ] **Step 4：更新 CMakeLists.txt，追加 MessageRepository.cpp 到 NovaChat_Core，追加测试文件**

```cmake
# client/CMakeLists.txt — NovaChat_Core 源列表
src/storage/MessageRepository.cpp

# client/tests/CMakeLists.txt — NovaChat_Tests 源列表
storage/test_message_repository.cpp
```

- [ ] **Step 5：运行测试**

```
cmake --build --preset windows-debug
ctest --preset windows-debug -V
```

预期：7 个 MessageRepository 测试 PASS，累计 38 个测试全部 PASS。

- [ ] **Step 6：提交**

```
git add client/src/storage/MessageRepository.h client/src/storage/MessageRepository.cpp
git add client/tests/storage/test_message_repository.cpp
git add client/CMakeLists.txt client/tests/CMakeLists.txt
git commit -m "feat(storage): 添加 MessageRepository，7 个单元测试通过"
```

---

## Task 25：客户端 ConversationRepository + 单元测试

**Files:**
- Create: `client/src/storage/ConversationRepository.h`
- Create: `client/src/storage/ConversationRepository.cpp`
- Create: `client/tests/storage/test_conversation_repository.cpp`

- [ ] **Step 1：写 client/src/storage/ConversationRepository.h**

```cpp
#pragma once
#include "core/Conversation.h"
#include "storage/DatabaseManager.h"
#include <string>
#include <vector>

// 操作 conversations 表
// 所有方法必须在 DB 线程调用
class ConversationRepository {
public:
    explicit ConversationRepository(DatabaseManager& db);

    // 插入或更新会话（按 owner_id + conversation_id 冲突时更新）
    void saveOrUpdate(const Conversation& conv);

    // 更新最新消息预览和时间
    void updateLastMessage(const std::string& owner_id,
                           const std::string& conversation_id,
                           const std::string& preview,
                           int64_t            timestamp_ms);

    // 未读数 +1
    void incrementUnread(const std::string& owner_id,
                         const std::string& conversation_id);

    // 清零未读数
    void clearUnread(const std::string& owner_id,
                     const std::string& conversation_id);

    // 按 last_message_time 倒序返回全部会话
    std::vector<Conversation> findAll(const std::string& owner_id);

private:
    DatabaseManager& db_;
    static Conversation rowToConversation(sqlite3_stmt* stmt);
};
```

- [ ] **Step 2：写 client/src/storage/ConversationRepository.cpp**

```cpp
#include "storage/ConversationRepository.h"
#include <sqlite3.h>

ConversationRepository::ConversationRepository(DatabaseManager& db) : db_(db) {}

Conversation ConversationRepository::rowToConversation(sqlite3_stmt* stmt) {
    Conversation c;
    c.owner_id             = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 0));
    c.conversation_id      = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 1));
    c.target_id            = reinterpret_cast<const char*>(sqlite3_column_text(stmt, 2));
    const unsigned char* p = sqlite3_column_text(stmt, 3);
    c.last_message_preview = p ? reinterpret_cast<const char*>(p) : "";
    c.last_message_time    = sqlite3_column_int64(stmt, 4);
    c.unread_count         = sqlite3_column_int  (stmt, 5);
    return c;
}

void ConversationRepository::saveOrUpdate(const Conversation& c) {
    const char* sql = R"(
        INSERT INTO conversations
            (owner_id, conversation_id, target_id,
             last_message_preview, last_message_time, unread_count)
        VALUES (?,?,?,?,?,?)
        ON CONFLICT(owner_id, conversation_id) DO UPDATE SET
            target_id             = excluded.target_id,
            last_message_preview  = excluded.last_message_preview,
            last_message_time     = excluded.last_message_time,
            unread_count          = excluded.unread_count
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text (stmt, 1, c.owner_id.c_str(),            -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 2, c.conversation_id.c_str(),     -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 3, c.target_id.c_str(),           -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 4, c.last_message_preview.c_str(),-1, SQLITE_STATIC);
    sqlite3_bind_int64(stmt, 5, c.last_message_time);
    sqlite3_bind_int  (stmt, 6, c.unread_count);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

void ConversationRepository::updateLastMessage(const std::string& owner_id,
                                                const std::string& conv_id,
                                                const std::string& preview,
                                                int64_t ts) {
    const char* sql = R"(
        UPDATE conversations
        SET last_message_preview = ?, last_message_time = ?
        WHERE owner_id = ? AND conversation_id = ?
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text (stmt, 1, preview.c_str(),  -1, SQLITE_STATIC);
    sqlite3_bind_int64(stmt, 2, ts);
    sqlite3_bind_text (stmt, 3, owner_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_bind_text (stmt, 4, conv_id.c_str(),  -1, SQLITE_STATIC);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

void ConversationRepository::incrementUnread(const std::string& owner_id,
                                              const std::string& conv_id) {
    const char* sql = R"(
        UPDATE conversations SET unread_count = unread_count + 1
        WHERE owner_id = ? AND conversation_id = ?
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text(stmt, 1, owner_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_bind_text(stmt, 2, conv_id.c_str(),  -1, SQLITE_STATIC);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

void ConversationRepository::clearUnread(const std::string& owner_id,
                                          const std::string& conv_id) {
    const char* sql = R"(
        UPDATE conversations SET unread_count = 0
        WHERE owner_id = ? AND conversation_id = ?
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text(stmt, 1, owner_id.c_str(), -1, SQLITE_STATIC);
    sqlite3_bind_text(stmt, 2, conv_id.c_str(),  -1, SQLITE_STATIC);
    sqlite3_step(stmt);
    sqlite3_finalize(stmt);
}

std::vector<Conversation>
ConversationRepository::findAll(const std::string& owner_id) {
    const char* sql = R"(
        SELECT owner_id, conversation_id, target_id,
               last_message_preview, last_message_time, unread_count
        FROM conversations WHERE owner_id = ?
        ORDER BY last_message_time DESC
    )";
    sqlite3_stmt* stmt;
    sqlite3_prepare_v2(db_.handle(), sql, -1, &stmt, nullptr);
    sqlite3_bind_text(stmt, 1, owner_id.c_str(), -1, SQLITE_STATIC);

    std::vector<Conversation> result;
    while (sqlite3_step(stmt) == SQLITE_ROW)
        result.push_back(rowToConversation(stmt));
    sqlite3_finalize(stmt);
    return result;
}
```

- [ ] **Step 3：写 client/tests/storage/test_conversation_repository.cpp**

```cpp
#include <catch2/catch_test_macros.hpp>
#include "storage/DatabaseManager.h"
#include "storage/ConversationRepository.h"

static Conversation makeConv(const std::string& owner,
                              const std::string& conv_id,
                              const std::string& target,
                              int64_t ts, int unread = 0) {
    Conversation c;
    c.owner_id            = owner;
    c.conversation_id     = conv_id;
    c.target_id           = target;
    c.last_message_preview = "last msg";
    c.last_message_time   = ts;
    c.unread_count        = unread;
    return c;
}

TEST_CASE("ConversationRepository saveOrUpdate 和 findAll", "[conv_repo]") {
    DatabaseManager        db(":memory:");
    ConversationRepository repo(db);

    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 1000));
    auto all = repo.findAll("o1");

    REQUIRE(all.size() == 1);
    CHECK(all[0].target_id == "alice");
}

TEST_CASE("ConversationRepository saveOrUpdate 更新已有会话", "[conv_repo]") {
    DatabaseManager        db(":memory:");
    ConversationRepository repo(db);

    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 1000));
    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 2000, 3));

    auto all = repo.findAll("o1");
    REQUIRE(all.size() == 1);
    CHECK(all[0].last_message_time == 2000);
    CHECK(all[0].unread_count      == 3);
}

TEST_CASE("ConversationRepository findAll 按 last_message_time 倒序", "[conv_repo]") {
    DatabaseManager        db(":memory:");
    ConversationRepository repo(db);

    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 1000));
    repo.saveOrUpdate(makeConv("o1", "c2", "bob",   3000));
    repo.saveOrUpdate(makeConv("o1", "c3", "carol", 2000));

    auto all = repo.findAll("o1");
    REQUIRE(all.size() == 3);
    CHECK(all[0].conversation_id == "c2");  // 3000 最新
    CHECK(all[1].conversation_id == "c3");
    CHECK(all[2].conversation_id == "c1");
}

TEST_CASE("ConversationRepository updateLastMessage", "[conv_repo]") {
    DatabaseManager        db(":memory:");
    ConversationRepository repo(db);

    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 1000));
    repo.updateLastMessage("o1", "c1", "新消息预览", 9999);

    auto all = repo.findAll("o1");
    REQUIRE(all.size() == 1);
    CHECK(all[0].last_message_preview == "新消息预览");
    CHECK(all[0].last_message_time    == 9999);
}

TEST_CASE("ConversationRepository incrementUnread 和 clearUnread", "[conv_repo]") {
    DatabaseManager        db(":memory:");
    ConversationRepository repo(db);

    repo.saveOrUpdate(makeConv("o1", "c1", "alice", 1000, 0));
    repo.incrementUnread("o1", "c1");
    repo.incrementUnread("o1", "c1");

    {
        auto all = repo.findAll("o1");
        CHECK(all[0].unread_count == 2);
    }

    repo.clearUnread("o1", "c1");
    {
        auto all = repo.findAll("o1");
        CHECK(all[0].unread_count == 0);
    }
}
```

- [ ] **Step 4：更新 CMakeLists.txt**

```cmake
# client/CMakeLists.txt — NovaChat_Core 追加
src/storage/ConversationRepository.cpp

# client/tests/CMakeLists.txt — NovaChat_Tests 追加
storage/test_conversation_repository.cpp
```

- [ ] **Step 5：运行测试**

```
cmake --build --preset windows-debug
ctest --preset windows-debug -V
```

预期：5 个 ConversationRepository 测试 PASS，累计 43 个测试全部 PASS。

- [ ] **Step 6：提交**

```
git add client/src/storage/ConversationRepository.h client/src/storage/ConversationRepository.cpp
git add client/tests/storage/test_conversation_repository.cpp
git add client/CMakeLists.txt client/tests/CMakeLists.txt
git commit -m "feat(storage): 添加 ConversationRepository，5 个单元测试通过"
```

---

## Task 26：客户端 ChatService

**Files:**
- Create: `client/src/service/ChatService.h`
- Create: `client/src/service/ChatService.cpp`

- [ ] **Step 1：写 client/src/service/ChatService.h**

```cpp
#pragma once
#include "core/Conversation.h"
#include "core/Message.h"
#include "core/User.h"
#include "network/TcpClient.h"
#include "storage/ConversationRepository.h"
#include "storage/MessageRepository.h"
#include <QObject>
#include <QString>
#include <vector>

class ChatService : public QObject {
    Q_OBJECT
public:
    ChatService(TcpClient*              client,
                MessageRepository&      msg_repo,
                ConversationRepository& conv_repo,
                const User&             current_user,
                QObject*                parent = nullptr);

    // 发送消息；消息先以 Sending 状态落库，回调后更新为 Sent/Failed
    void sendMessage(const std::string& receiver_id,
                     const std::string& content);

    // 加载历史消息；before_timestamp=0 表示最新一页
    void loadHistory(const std::string& conversation_id,
                     int64_t            before_timestamp = 0);

    // 从服务端同步会话列表（登录后调用一次）
    void syncConversations();

    // 本地会话列表缓存
    const std::vector<Conversation>& conversations() const { return conversations_; }

    // 生成确定性会话 ID：min(a,b) + ":" + max(a,b)
    static std::string makeConvId(const std::string& a, const std::string& b);

signals:
    void messageSent       (Message message);
    void messageFailed     (std::string local_id);
    void messageReceived   (Message message);
    void historyLoaded     (std::string conv_id,
                            std::vector<Message> messages,
                            bool has_more);
    void conversationsLoaded(std::vector<Conversation> conversations);

private slots:
    void onPacketReceived(Packet pkt);   // 处理 seq_id==0 的服务端推送

private:
    void handleMessagePush(const Packet& pkt);

    TcpClient*              client_;
    MessageRepository&      msg_repo_;
    ConversationRepository& conv_repo_;
    const User&             current_user_;
    std::vector<Conversation> conversations_;

    uint64_t local_id_counter_{0};   // 本地消息 ID 计数器
};
```

- [ ] **Step 2：写 client/src/service/ChatService.cpp**

```cpp
#include "service/ChatService.h"
#include "logger/Logger.h"
#include "proto/MessageType.h"
#include "chat.pb.h"
#include "common.pb.h"
#include <algorithm>
#include <chrono>

using namespace std::chrono;

static int64_t nowMs() {
    return duration_cast<milliseconds>(
        system_clock::now().time_since_epoch()).count();
}

ChatService::ChatService(TcpClient*              client,
                          MessageRepository&      msg_repo,
                          ConversationRepository& conv_repo,
                          const User&             current_user,
                          QObject*                parent)
    : QObject(parent)
    , client_(client)
    , msg_repo_(msg_repo)
    , conv_repo_(conv_repo)
    , current_user_(current_user)
{
    conversations_ = conv_repo_.findAll(current_user_.server_id);

    // 接收服务端主动推送（seq_id == 0 的包经 TcpClient::packetReceived 分发）
    connect(client_, &TcpClient::packetReceived,
            this,    &ChatService::onPacketReceived,
            Qt::QueuedConnection);
}

std::string ChatService::makeConvId(const std::string& a,
                                     const std::string& b) {
    return (a < b) ? (a + ":" + b) : (b + ":" + a);
}

void ChatService::sendMessage(const std::string& receiver_id,
                               const std::string& content) {
    const std::string conv_id =
        makeConvId(current_user_.server_id, receiver_id);

    // 生成本地临时 ID
    const std::string local_id =
        "local_" + std::to_string(++local_id_counter_)
        + "_" + std::to_string(nowMs());

    // 先落库（Sending 状态）
    Message local_msg;
    local_msg.message_id      = local_id;
    local_msg.conversation_id = conv_id;
    local_msg.sender_id       = current_user_.server_id;
    local_msg.content         = content;
    local_msg.type            = MessageType::Text;
    local_msg.timestamp_ms    = nowMs();
    local_msg.status          = MessageStatus::Sending;
    msg_repo_.save(local_msg);

    // 更新会话预览
    Conversation conv;
    conv.owner_id            = current_user_.server_id;
    conv.conversation_id     = conv_id;
    conv.target_id           = receiver_id;
    conv.last_message_preview = content.size() > 50
                                ? content.substr(0, 50) + "..."
                                : content;
    conv.last_message_time   = local_msg.timestamp_ms;
    conv.unread_count        = 0;
    conv_repo_.saveOrUpdate(conv);

    emit messageSent(local_msg);

    // 发送请求
    novachat::SendMessageRequest req;
    req.set_token       (current_user_.token);
    req.set_local_msg_id(local_id);
    req.set_receiver_id (receiver_id);
    req.set_content     (content);

    Packet pkt;
    pkt.type = MessageType::SendMessageRequest;
    pkt.payload.resize(req.ByteSizeLong());
    req.SerializeToArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()));

    client_->sendPacket(std::move(pkt), [this, local_id](Packet resp) {
        if (resp.type == MessageType::SendMessageResponse) {
            novachat::SendMessageResponse r;
            if (!r.ParseFromArray(resp.payload.data(),
                                  static_cast<int>(resp.payload.size()))) return;
            msg_repo_.confirmSent(local_id, r.server_msg_id(), r.timestamp_ms());
            conv_repo_.updateLastMessage(current_user_.server_id,
                                         r.conversation_id(), {}, r.timestamp_ms());
            Logger::debug("Message confirmed: {} → {}", local_id, r.server_msg_id());
        } else {
            msg_repo_.updateStatus(local_id, static_cast<int>(MessageStatus::Failed));
            emit messageFailed(local_id);
        }
    });
}

void ChatService::loadHistory(const std::string& conv_id,
                               int64_t before_ts) {
    novachat::GetHistoryRequest req;
    req.set_token           (current_user_.token);
    req.set_conversation_id (conv_id);
    req.set_before_timestamp(before_ts);
    req.set_limit           (50);

    Packet pkt;
    pkt.type = MessageType::GetHistoryRequest;
    pkt.payload.resize(req.ByteSizeLong());
    req.SerializeToArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()));

    client_->sendPacket(std::move(pkt), [this, conv_id](Packet resp) {
        if (resp.type != MessageType::GetHistoryResponse) return;

        novachat::GetHistoryResponse r;
        if (!r.ParseFromArray(resp.payload.data(),
                              static_cast<int>(resp.payload.size()))) return;

        std::vector<Message> msgs;
        for (const auto& m : r.messages()) {
            Message msg;
            msg.message_id      = m.message_id();
            msg.conversation_id = m.conversation_id();
            msg.sender_id       = m.sender_id();
            msg.content         = m.content();
            msg.type            = MessageType::Text;
            msg.timestamp_ms    = m.timestamp_ms();
            msg.status          = MessageStatus::Sent;
            msg_repo_.save(msg);
            msgs.push_back(msg);
        }

        emit historyLoaded(conv_id, msgs, r.has_more());
    });
}

void ChatService::syncConversations() {
    novachat::GetConversationsRequest req;
    req.set_token(current_user_.token);

    Packet pkt;
    pkt.type = MessageType::GetConversationsRequest;
    pkt.payload.resize(req.ByteSizeLong());
    req.SerializeToArray(pkt.payload.data(), static_cast<int>(pkt.payload.size()));

    client_->sendPacket(std::move(pkt), [this](Packet resp) {
        if (resp.type != MessageType::GetConversationsResponse) return;

        novachat::GetConversationsResponse r;
        if (!r.ParseFromArray(resp.payload.data(),
                              static_cast<int>(resp.payload.size()))) return;

        for (const auto& c : r.conversations()) {
            Conversation conv;
            conv.owner_id            = current_user_.server_id;
            conv.conversation_id     = c.conversation_id();
            conv.target_id           = c.peer_user_id();
            conv.last_message_preview = c.last_message_preview();
            conv.last_message_time   = c.last_message_time();
            conv.unread_count        = 0;
            conv_repo_.saveOrUpdate(conv);
        }

        conversations_ = conv_repo_.findAll(current_user_.server_id);
        emit conversationsLoaded(conversations_);
        Logger::info("Conversations synced: {}", conversations_.size());
    });
}

void ChatService::onPacketReceived(Packet pkt) {
    if (pkt.type == MessageType::MessagePush) {
        handleMessagePush(pkt);
    }
}

void ChatService::handleMessagePush(const Packet& pkt) {
    novachat::MessagePush push;
    if (!push.ParseFromArray(pkt.payload.data(),
                             static_cast<int>(pkt.payload.size()))) return;

    Message msg;
    msg.message_id      = push.message_id();
    msg.conversation_id = push.conversation_id();
    msg.sender_id       = push.sender_id();
    msg.content         = push.content();
    msg.type            = MessageType::Text;
    msg.timestamp_ms    = push.timestamp_ms();
    msg.status          = MessageStatus::Sent;

    msg_repo_.save(msg);

    // 更新会话预览 + 未读数
    conv_repo_.updateLastMessage(current_user_.server_id,
                                  msg.conversation_id,
                                  msg.content, msg.timestamp_ms);
    conv_repo_.incrementUnread(current_user_.server_id, msg.conversation_id);

    // 刷新本地缓存
    conversations_ = conv_repo_.findAll(current_user_.server_id);

    Logger::debug("Message push received from {}", msg.sender_id);
    emit messageReceived(msg);
}
```

- [ ] **Step 3：更新 client/CMakeLists.txt，追加 ChatService.cpp 到 NovaChat_Core**

```cmake
src/service/ChatService.cpp
```

- [ ] **Step 4：编译验证**

```
cmake --build --preset windows-debug
```

预期：编译无报错。

- [ ] **Step 5：提交**

```
git add client/src/service/ChatService.h client/src/service/ChatService.cpp
git add client/CMakeLists.txt
git commit -m "feat(service): 添加 ChatService，封装发送/接收/历史/会话同步"
```

---

## Task 27：MessageBubbleDelegate + ChatView

**Files:**
- Create: `client/src/ui/MessageBubbleDelegate.h`
- Create: `client/src/ui/MessageBubbleDelegate.cpp`
- Create: `client/src/ui/ChatView.h`
- Create: `client/src/ui/ChatView.cpp`

- [ ] **Step 1：写 client/src/ui/MessageBubbleDelegate.h**

```cpp
#pragma once
#include <QStyledItemDelegate>

// 自定义消息气泡渲染
// Item roles（对应 Qt::UserRole + N）:
//   UserRole+1  — content (QString)
//   UserRole+2  — sender_id (QString)
//   UserRole+3  — timestamp_ms (qlonglong)
//   UserRole+4  — is_outgoing (bool)
//   UserRole+5  — status (int: 0=发送中 1=已发送 2=失败)
class MessageBubbleDelegate : public QStyledItemDelegate {
    Q_OBJECT
public:
    explicit MessageBubbleDelegate(const QString& own_user_id,
                                    QObject* parent = nullptr);

    void  paint    (QPainter*                   painter,
                    const QStyleOptionViewItem& option,
                    const QModelIndex&          index) const override;

    QSize sizeHint (const QStyleOptionViewItem& option,
                    const QModelIndex&          index) const override;

private:
    // 根据视图宽度计算气泡宽度上限
    static constexpr int kBubbleMaxWidthRatio = 65;  // 占视图宽度的 65%
    static constexpr int kPadH = 12;  // 气泡水平内边距
    static constexpr int kPadV = 8;   // 气泡垂直内边距
    static constexpr int kRadius = 10; // 圆角半径
    static constexpr int kItemVMargin = 6; // item 上下外边距

    QString own_user_id_;
};
```

- [ ] **Step 2：写 client/src/ui/MessageBubbleDelegate.cpp**

```cpp
#include "ui/MessageBubbleDelegate.h"
#include <QDateTime>
#include <QFontMetrics>
#include <QPainter>
#include <QPainterPath>
#include <QTextOption>

namespace Role {
    constexpr int Content    = Qt::UserRole + 1;
    constexpr int SenderId   = Qt::UserRole + 2;
    constexpr int Timestamp  = Qt::UserRole + 3;
    constexpr int IsOutgoing = Qt::UserRole + 4;
    constexpr int Status     = Qt::UserRole + 5;
}

MessageBubbleDelegate::MessageBubbleDelegate(const QString& own_user_id,
                                               QObject* parent)
    : QStyledItemDelegate(parent), own_user_id_(own_user_id) {}

void MessageBubbleDelegate::paint(QPainter*                   painter,
                                   const QStyleOptionViewItem& option,
                                   const QModelIndex&          index) const {
    painter->save();
    painter->setRenderHint(QPainter::Antialiasing);

    const bool   is_out  = index.data(Role::IsOutgoing).toBool();
    const QString text   = index.data(Role::Content).toString();
    const int64_t ts_ms  = index.data(Role::Timestamp).toLongLong();
    const int     status = index.data(Role::Status).toInt();

    const QDateTime dt   = QDateTime::fromMSecsSinceEpoch(ts_ms);
    const QString ts_str = dt.toString("HH:mm");
    const QString foot   = is_out
        ? (status == 2 ? "发送失败 " + ts_str : ts_str)
        : ts_str;

    const QRect total_rect = option.rect.adjusted(8, kItemVMargin,
                                                  -8, -kItemVMargin);
    const int max_bubble_w = total_rect.width() * kBubbleMaxWidthRatio / 100;

    QFont font = option.font;
    QFontMetrics fm(font);
    QFont small_font = font;
    small_font.setPointSize(font.pointSize() - 2);
    QFontMetrics small_fm(small_font);

    // 计算文字包裹后的尺寸
    const int text_max_w = max_bubble_w - 2 * kPadH;
    const QRect text_bounding = fm.boundingRect(
        QRect(0, 0, text_max_w, 9999),
        Qt::TextWordWrap, text);
    const int bubble_w = text_bounding.width() + 2 * kPadH;
    const int bubble_h = text_bounding.height() + 2 * kPadV;

    // 定位气泡
    int bubble_x = is_out
        ? total_rect.right() - bubble_w
        : total_rect.left();
    QRect bubble_rect(bubble_x, total_rect.top(), bubble_w, bubble_h);

    // 绘制气泡背景
    const QColor bubble_color = is_out ? QColor(0x07, 0xC1, 0x60)
                                        : QColor(0xFF, 0xFF, 0xFF);
    painter->setPen(Qt::NoPen);
    painter->setBrush(bubble_color);
    painter->drawRoundedRect(bubble_rect, kRadius, kRadius);

    // 绘制文字
    const QColor text_color = is_out ? Qt::white : Qt::black;
    painter->setPen(text_color);
    painter->setFont(font);
    painter->drawText(bubble_rect.adjusted(kPadH, kPadV, -kPadH, -kPadV),
                      Qt::TextWordWrap, text);

    // 绘制时间戳
    painter->setFont(small_font);
    painter->setPen(QColor(0xAA, 0xAA, 0xAA));
    const int foot_y = bubble_rect.bottom() + 2;
    if (is_out) {
        painter->drawText(
            QRect(total_rect.left(), foot_y, total_rect.width(), 20),
            Qt::AlignRight | Qt::AlignTop, foot);
    } else {
        painter->drawText(
            QRect(bubble_rect.left(), foot_y, total_rect.width(), 20),
            Qt::AlignLeft | Qt::AlignTop, foot);
    }

    painter->restore();
}

QSize MessageBubbleDelegate::sizeHint(const QStyleOptionViewItem& option,
                                       const QModelIndex&          index) const {
    const QString text = index.data(Role::Content).toString();
    const int view_w   = option.rect.isValid() ? option.rect.width() : 500;
    const int max_bubble_w = view_w * kBubbleMaxWidthRatio / 100;
    const int text_max_w   = max_bubble_w - 2 * kPadH;

    QFontMetrics fm(option.font);
    const QRect text_bounding = fm.boundingRect(
        QRect(0, 0, text_max_w, 9999),
        Qt::TextWordWrap, text);

    const int total_h = kItemVMargin
                       + kPadV + text_bounding.height() + kPadV
                       + 20   // 时间戳行高
                       + kItemVMargin;
    return {view_w, total_h};
}
```

- [ ] **Step 3：写 client/src/ui/ChatView.h**

```cpp
#pragma once
#include "core/Message.h"
#include "service/ChatService.h"
#include "ui/MessageBubbleDelegate.h"
#include <QListView>
#include <QLineEdit>
#include <QPushButton>
#include <QStandardItemModel>
#include <QString>
#include <QWidget>

// 单个会话的聊天区域
// · 上方：QListView（气泡列表，支持分页上滑加载）
// · 下方：输入框 + 发送按钮
class ChatView : public QWidget {
    Q_OBJECT
public:
    explicit ChatView(ChatService*  service,
                      const QString& own_user_id,
                      QWidget*      parent = nullptr);

    // 切换会话（清除当前内容并加载新会话的历史）
    void openConversation(const std::string& conversation_id,
                          const std::string& peer_user_id);

signals:
    void messageSent(const std::string& conversation_id);

private slots:
    void onSendClicked();
    void onHistoryLoaded(const std::string& conv_id,
                         const std::vector<Message>& messages,
                         bool has_more);
    void onMessageReceived(const Message& msg);
    void onMessageSent    (const Message& msg);

private:
    void appendMessage(const Message& msg);
    void scrollToBottom();

    ChatService*          service_;
    QString               own_user_id_;
    std::string           current_conv_id_;

    QListView*            message_list_;
    QStandardItemModel*   model_;
    MessageBubbleDelegate* delegate_;
    QLineEdit*            input_edit_;
    QPushButton*          send_btn_;

    bool has_more_{false};
    bool loading_history_{false};
};
```

- [ ] **Step 4：写 client/src/ui/ChatView.cpp**

```cpp
#include "ui/ChatView.h"
#include <QDateTime>
#include <QKeyEvent>
#include <QScrollBar>
#include <QStandardItem>
#include <QVBoxLayout>
#include <QHBoxLayout>
#include <QLabel>

namespace Role {
    constexpr int Content    = Qt::UserRole + 1;
    constexpr int SenderId   = Qt::UserRole + 2;
    constexpr int Timestamp  = Qt::UserRole + 3;
    constexpr int IsOutgoing = Qt::UserRole + 4;
    constexpr int Status     = Qt::UserRole + 5;
}

ChatView::ChatView(ChatService*  service,
                    const QString& own_user_id,
                    QWidget*      parent)
    : QWidget(parent)
    , service_(service)
    , own_user_id_(own_user_id)
{
    model_    = new QStandardItemModel(this);
    delegate_ = new MessageBubbleDelegate(own_user_id_, this);

    message_list_ = new QListView(this);
    message_list_->setModel(model_);
    message_list_->setItemDelegate(delegate_);
    message_list_->setSpacing(2);
    message_list_->setEditTriggers(QAbstractItemView::NoEditTriggers);
    message_list_->setSelectionMode(QAbstractItemView::NoSelection);
    message_list_->setVerticalScrollMode(QAbstractItemView::ScrollPerPixel);
    message_list_->setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOff);

    input_edit_ = new QLineEdit(this);
    send_btn_   = new QPushButton("发送", this);
    input_edit_->setPlaceholderText("输入消息，Enter 发送...");

    // 输入框支持 Enter 发送
    input_edit_->installEventFilter(this);

    auto* bottom = new QHBoxLayout;
    bottom->addWidget(input_edit_);
    bottom->addWidget(send_btn_);

    auto* layout = new QVBoxLayout(this);
    layout->setContentsMargins(0, 0, 0, 0);
    layout->setSpacing(0);
    layout->addWidget(message_list_);
    layout->addLayout(bottom);

    connect(send_btn_, &QPushButton::clicked, this, &ChatView::onSendClicked);
    connect(service_, &ChatService::historyLoaded,
            this,     &ChatView::onHistoryLoaded,
            Qt::QueuedConnection);
    connect(service_, &ChatService::messageReceived,
            this,     &ChatView::onMessageReceived,
            Qt::QueuedConnection);
    connect(service_, &ChatService::messageSent,
            this,     &ChatView::onMessageSent,
            Qt::QueuedConnection);

    // 滚动到顶部时加载更多历史
    connect(message_list_->verticalScrollBar(), &QScrollBar::valueChanged,
            this, [this](int value) {
                if (value == 0 && has_more_ && !loading_history_) {
                    loading_history_ = true;
                    // 取最早一条消息的时间戳作为 before_timestamp
                    if (model_->rowCount() > 0) {
                        const int64_t earliest = model_->item(0)
                            ->data(Role::Timestamp).toLongLong();
                        service_->loadHistory(current_conv_id_, earliest);
                    }
                }
            });
}

bool ChatView::eventFilter(QObject* obj, QEvent* event) {
    if (obj == input_edit_ && event->type() == QEvent::KeyPress) {
        auto* ke = static_cast<QKeyEvent*>(event);
        if (ke->key() == Qt::Key_Return && !(ke->modifiers() & Qt::ShiftModifier)) {
            onSendClicked();
            return true;
        }
    }
    return QWidget::eventFilter(obj, event);
}

void ChatView::openConversation(const std::string& conv_id,
                                 const std::string& /*peer_user_id*/) {
    current_conv_id_ = conv_id;
    model_->clear();
    has_more_       = false;
    loading_history_ = false;

    // 先从本地 DB 加载（若已有缓存，可以在 loadHistory 回调中填充）
    service_->loadHistory(conv_id, 0);
}

void ChatView::onSendClicked() {
    if (current_conv_id_.empty()) return;
    const QString text = input_edit_->text().trimmed();
    if (text.isEmpty()) return;
    input_edit_->clear();

    // 从 conv_id 解析接收方
    const auto colon = current_conv_id_.find(':');
    const std::string id_a = current_conv_id_.substr(0, colon);
    const std::string id_b = current_conv_id_.substr(colon + 1);
    const std::string receiver = (id_a == own_user_id_.toStdString()) ? id_b : id_a;

    service_->sendMessage(receiver, text.toStdString());
}

void ChatView::onHistoryLoaded(const std::string& conv_id,
                                const std::vector<Message>& messages,
                                bool has_more) {
    if (conv_id != current_conv_id_) return;

    loading_history_ = false;
    has_more_        = has_more;

    if (messages.empty()) return;

    // 保存当前滚动位置（加载早期消息后维持视图稳定）
    auto* sb = message_list_->verticalScrollBar();
    const int old_max = sb->maximum();

    // 在 model 顶部插入历史消息（升序）
    for (int i = 0; i < static_cast<int>(messages.size()); ++i) {
        auto* item = new QStandardItem;
        const Message& m = messages[i];
        item->setData(QString::fromStdString(m.content),       Role::Content);
        item->setData(QString::fromStdString(m.sender_id),     Role::SenderId);
        item->setData(static_cast<qlonglong>(m.timestamp_ms),  Role::Timestamp);
        item->setData(m.sender_id == own_user_id_.toStdString(), Role::IsOutgoing);
        item->setData(static_cast<int>(m.status),              Role::Status);
        model_->insertRow(i, item);
    }

    // 若是首次加载，滚动到底部；若是分页加载，维持视图
    if (old_max == 0) {
        scrollToBottom();
    } else {
        const int new_max = sb->maximum();
        sb->setValue(sb->value() + (new_max - old_max));
    }
}

void ChatView::onMessageReceived(const Message& msg) {
    if (msg.conversation_id != current_conv_id_) return;
    appendMessage(msg);
}

void ChatView::onMessageSent(const Message& msg) {
    if (msg.conversation_id != current_conv_id_) return;
    appendMessage(msg);
}

void ChatView::appendMessage(const Message& msg) {
    auto* item = new QStandardItem;
    item->setData(QString::fromStdString(msg.content),          Role::Content);
    item->setData(QString::fromStdString(msg.sender_id),        Role::SenderId);
    item->setData(static_cast<qlonglong>(msg.timestamp_ms),     Role::Timestamp);
    item->setData(msg.sender_id == own_user_id_.toStdString(),  Role::IsOutgoing);
    item->setData(static_cast<int>(msg.status),                 Role::Status);
    model_->appendRow(item);
    scrollToBottom();
}

void ChatView::scrollToBottom() {
    QMetaObject::invokeMethod(this, [this] {
        auto* sb = message_list_->verticalScrollBar();
        sb->setValue(sb->maximum());
    }, Qt::QueuedConnection);
}
```

- [ ] **Step 5：更新 client/CMakeLists.txt，追加 UI 文件到 NovaChat_UI**

```cmake
add_library(NovaChat_UI STATIC
    ...
    src/ui/MessageBubbleDelegate.cpp   # ← 新增
    src/ui/ChatView.cpp                # ← 新增
)
```

- [ ] **Step 6：编译验证**

```
cmake --build --preset windows-debug
```

- [ ] **Step 7：提交**

```
git add client/src/ui/MessageBubbleDelegate.h client/src/ui/MessageBubbleDelegate.cpp
git add client/src/ui/ChatView.h client/src/ui/ChatView.cpp
git add client/CMakeLists.txt
git commit -m "feat(ui): 添加 MessageBubbleDelegate 和 ChatView"
```

---

## Task 28：ConversationList + MainWindow 集成

**Files:**
- Create: `client/src/ui/ConversationList.h`
- Create: `client/src/ui/ConversationList.cpp`
- Modify: `client/src/ui/MainWindow.h`
- Modify: `client/src/ui/MainWindow.cpp`
- Modify: `client/src/main.cpp`

- [ ] **Step 1：写 client/src/ui/ConversationList.h**

```cpp
#pragma once
#include "core/Conversation.h"
#include "service/ChatService.h"
#include <QListWidget>
#include <QWidget>
#include <vector>

// 最近会话列表
// · 按 last_message_time 倒序展示
// · 显示未读数徽标（item 右侧）
// · 双击打开对应会话
class ConversationList : public QWidget {
    Q_OBJECT
public:
    explicit ConversationList(ChatService* service,
                               QWidget*     parent = nullptr);

signals:
    void conversationSelected(std::string conversation_id,
                               std::string peer_user_id);

private slots:
    void onConversationsLoaded(const std::vector<Conversation>& convs);
    void onMessageReceived    (const Message& msg);
    void onItemDoubleClicked  (QListWidgetItem* item);

private:
    void refresh(const std::vector<Conversation>& convs);

    ChatService* service_;
    QListWidget* list_;
    std::vector<Conversation> conversations_;
};
```

- [ ] **Step 2：写 client/src/ui/ConversationList.cpp**

```cpp
#include "ui/ConversationList.h"
#include <QDateTime>
#include <QHBoxLayout>
#include <QLabel>
#include <QVBoxLayout>

ConversationList::ConversationList(ChatService* service, QWidget* parent)
    : QWidget(parent)
    , service_(service)
{
    list_ = new QListWidget(this);

    auto* layout = new QVBoxLayout(this);
    layout->setContentsMargins(0, 0, 0, 0);
    layout->addWidget(list_);

    conversations_ = service_->conversations();
    refresh(conversations_);

    connect(service_, &ChatService::conversationsLoaded,
            this,     &ConversationList::onConversationsLoaded,
            Qt::QueuedConnection);
    connect(service_, &ChatService::messageReceived,
            this,     &ConversationList::onMessageReceived,
            Qt::QueuedConnection);
    connect(list_, &QListWidget::itemDoubleClicked,
            this,  &ConversationList::onItemDoubleClicked);
}

void ConversationList::refresh(const std::vector<Conversation>& convs) {
    list_->clear();
    conversations_ = convs;
    for (const auto& c : convs) {
        const QString ts = QDateTime::fromMSecsSinceEpoch(c.last_message_time)
                               .toString("MM/dd HH:mm");
        QString display = QString("[%1] %2\n%3")
            .arg(QString::fromStdString(c.target_id))
            .arg(ts)
            .arg(QString::fromStdString(c.last_message_preview));
        if (c.unread_count > 0) {
            display += QString("  [%1]").arg(c.unread_count);
        }
        auto* item = new QListWidgetItem(display, list_);
        item->setData(Qt::UserRole,     QString::fromStdString(c.conversation_id));
        item->setData(Qt::UserRole + 1, QString::fromStdString(c.target_id));
    }
}

void ConversationList::onConversationsLoaded(const std::vector<Conversation>& convs) {
    refresh(convs);
}

void ConversationList::onMessageReceived(const Message& msg) {
    // 新消息到达时刷新会话列表（ChatService 已更新 conversations()）
    refresh(service_->conversations());
}

void ConversationList::onItemDoubleClicked(QListWidgetItem* item) {
    if (!item) return;
    const std::string conv_id  = item->data(Qt::UserRole).toString().toStdString();
    const std::string peer_id  = item->data(Qt::UserRole + 1).toString().toStdString();
    emit conversationSelected(conv_id, peer_id);
}
```

- [ ] **Step 3：更新 client/src/ui/MainWindow.h**

```cpp
#pragma once
#include "core/User.h"
#include "service/AuthService.h"
#include "service/ChatService.h"
#include "service/ContactService.h"
#include "ui/ChatView.h"
#include "ui/ContactPanel.h"
#include "ui/ConversationList.h"
#include <QMainWindow>
#include <QTabWidget>
#include <QSplitter>

class MainWindow : public QMainWindow {
    Q_OBJECT
public:
    explicit MainWindow(const User&      user,
                         AuthService*     auth_service,
                         ContactService*  contact_service,
                         ChatService*     chat_service,
                         QWidget*         parent = nullptr);
    ~MainWindow() override = default;

private slots:
    void onLogout();
    void onConversationSelected(const std::string& conv_id,
                                 const std::string& peer_user_id);
    void onContactSelected(const Contact& contact);

private:
    User             current_user_;
    AuthService*     auth_service_;
    ContactService*  contact_service_;
    ChatService*     chat_service_;

    QTabWidget*      left_tabs_;
    ConversationList* conv_list_;
    ContactPanel*    contact_panel_;
    ChatView*        chat_view_;
};
```

- [ ] **Step 4：更新 client/src/ui/MainWindow.cpp**

```cpp
#include "ui/MainWindow.h"
#include "ui/LoginWindow.h"
#include <QApplication>
#include <QLabel>
#include <QMenuBar>
#include <QSplitter>
#include <QStatusBar>

MainWindow::MainWindow(const User&      user,
                        AuthService*     auth_service,
                        ContactService*  contact_service,
                        ChatService*     chat_service,
                        QWidget*         parent)
    : QMainWindow(parent)
    , current_user_(user)
    , auth_service_(auth_service)
    , contact_service_(contact_service)
    , chat_service_(chat_service)
{
    setWindowTitle(QString("NovaChat — %1")
                   .arg(QString::fromStdString(user.display_name)));
    setMinimumSize(1100, 680);

    // ── 菜单 ─────────────────────────────────────────────────────
    auto* file_menu  = menuBar()->addMenu("文件");
    auto* logout_act = file_menu->addAction("退出登录");
    auto* quit_act   = file_menu->addAction("退出程序");
    connect(logout_act, &QAction::triggered, this, &MainWindow::onLogout);
    connect(quit_act,   &QAction::triggered, qApp, &QApplication::quit);

    // ── 左侧：Tab（会话 | 联系人）────────────────────────────────
    conv_list_    = new ConversationList(chat_service_, this);
    contact_panel_ = new ContactPanel(contact_service_, this);

    left_tabs_ = new QTabWidget(this);
    left_tabs_->addTab(conv_list_,     "💬 会话");
    left_tabs_->addTab(contact_panel_, "👥 联系人");
    left_tabs_->setFixedWidth(300);

    // ── 右侧：ChatView ───────────────────────────────────────────
    chat_view_ = new ChatView(
        chat_service_,
        QString::fromStdString(user.server_id),
        this);

    auto* splitter = new QSplitter(Qt::Horizontal, this);
    splitter->addWidget(left_tabs_);
    splitter->addWidget(chat_view_);
    splitter->setStretchFactor(0, 0);
    splitter->setStretchFactor(1, 1);
    setCentralWidget(splitter);

    // ── 信号连接 ─────────────────────────────────────────────────
    connect(conv_list_, &ConversationList::conversationSelected,
            this,       &MainWindow::onConversationSelected);
    connect(contact_panel_, &ContactPanel::contactSelected,
            this,           &MainWindow::onContactSelected);

    statusBar()->showMessage(
        QString("已登录：%1").arg(QString::fromStdString(user.username)));
}

void MainWindow::onLogout() {
    auth_service_->logout();
    auto* login_win = new LoginWindow(auth_service_);
    login_win->show();
    close();
}

void MainWindow::onConversationSelected(const std::string& conv_id,
                                         const std::string& peer_user_id) {
    chat_view_->openConversation(conv_id, peer_user_id);
    chat_service_->conv_repo_ref().clearUnread(       // 清零未读（见下方说明）
        current_user_.server_id, conv_id);
}

void MainWindow::onContactSelected(const Contact& contact) {
    // 从联系人发起聊天：生成 conv_id 并打开
    const std::string conv_id =
        ChatService::makeConvId(current_user_.server_id, contact.user_id);
    chat_view_->openConversation(conv_id, contact.user_id);
    left_tabs_->setCurrentIndex(0);   // 切换到会话 tab
}
```

> **说明：** `onConversationSelected` 需要调用 `ConversationRepository::clearUnread`。为了避免在 MainWindow 中直接持有 repo 引用，在 ChatService 中暴露一个 `clearUnread(conv_id)` 方法（修改 ChatService.h/.cpp 追加一行），内部转发给 `conv_repo_`。

在 `ChatService.h` 追加：
```cpp
    void clearUnread(const std::string& conversation_id);
```

在 `ChatService.cpp` 追加：
```cpp
void ChatService::clearUnread(const std::string& conv_id) {
    conv_repo_.clearUnread(current_user_.server_id, conv_id);
}
```

将 `MainWindow::onConversationSelected` 中的调用改为：
```cpp
chat_service_->clearUnread(conv_id);
```

- [ ] **Step 5：更新 client/src/main.cpp（创建 ChatService，传入 MainWindow）**

```cpp
#include "storage/MessageRepository.h"
#include "storage/ConversationRepository.h"
#include "service/ChatService.h"
// ... （其他已有头文件）

int main(int argc, char* argv[]) {
    // ... Logger / Config / DatabaseManager 初始化（同阶段四）

    auto db           = std::make_unique<DatabaseManager>(db_path);
    auto user_repo    = std::make_unique<UserRepository>(*db);
    auto contact_repo = std::make_unique<ContactRepository>(*db);
    auto msg_repo     = std::make_unique<MessageRepository>(*db);
    auto conv_repo    = std::make_unique<ConversationRepository>(*db);

    auto* tcp_client = new TcpClient(&app);

    auto launch_main = [&](const User& user) -> MainWindow* {
        auto* auth_svc    = new AuthService(tcp_client, *user_repo, &app);
        auto* contact_svc = new ContactService(tcp_client, *contact_repo, user, &app);
        auto* chat_svc    = new ChatService(tcp_client, *msg_repo, *conv_repo, user, &app);

        auto* win = new MainWindow(user, auth_svc, contact_svc, chat_svc);
        win->show();

        QObject::connect(tcp_client, &TcpClient::connected,
                         contact_svc, &ContactService::syncContacts);
        QObject::connect(tcp_client, &TcpClient::connected,
                         chat_svc,   &ChatService::syncConversations);
        return win;
    };

    // auto-login 和 LoginWindow 逻辑同阶段四...

    tcp_client->connectToServer(AppConfig::instance().server.host,
                                 AppConfig::instance().server.port);
    const int result = app.exec();
    Logger::info("NovaChat 退出");
    Logger::shutdown();
    return result;
}
```

- [ ] **Step 6：更新 client/CMakeLists.txt，追加 ConversationList.cpp 到 NovaChat_UI**

```cmake
src/ui/ConversationList.cpp    # ← 新增
```

- [ ] **Step 7：编译验证**

```
cmake --build --preset windows-debug
```

预期：编译无报错。

- [ ] **Step 8：端到端聊天流程验证**

**启动服务端：**
```
./build/debug/server/NovaChatServer.exe
```

**客户端 A（alice）：**
1. 注册/登录，主窗口显示两个 Tab（会话/联系人）
2. 联系人 Tab 添加好友 bob
3. 点击 bob 的联系人 → 右侧打开空白聊天区
4. 输入 "Hello Bob!" → 发送 → 气泡出现在右侧（绿色）

**客户端 B（bob）：**
1. 登录后，左侧会话 Tab 出现与 alice 的会话（含未读徽标）
2. 双击会话 → 右侧显示 "Hello Bob!" 气泡（左侧灰色）
3. 回复 "Hi Alice!" → alice 端实时收到

**重启验证：**
- 重启 alice 客户端 → 历史消息从本地 SQLite 加载，无需重新请求服务端

- [ ] **Step 9：运行全部测试**

```
cmake --build --preset windows-debug
ctest --preset windows-debug -V
```

预期：43 个测试全部 PASS。

- [ ] **Step 10：提交**

```
git add client/src/ui/ConversationList.h client/src/ui/ConversationList.cpp
git add client/src/ui/MainWindow.h client/src/ui/MainWindow.cpp
git add client/src/main.cpp client/CMakeLists.txt
git commit -m "feat: 完成聊天模块——实时收发、气泡UI、会话列表、历史分页"
```

---

## 阶段五完成标准

- [ ] `ctest --preset windows-debug` 43 个测试全部 PASS
- [ ] 两个客户端实例可实时收发文字消息（绿/灰气泡）
- [ ] 消息推送在接收方在线时实时到达（`MessagePush`）
- [ ] ConversationList 显示未读数，打开会话后清零
- [ ] 滚动到顶部自动加载更多历史（每次 50 条）
- [ ] 重启客户端后历史消息从本地 SQLite 恢复
- [ ] 从联系人 Tab 点击好友可直接发起聊天
- [ ] 所有代码已提交到 git
